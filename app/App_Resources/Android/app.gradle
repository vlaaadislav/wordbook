// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "wordbook.application"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
